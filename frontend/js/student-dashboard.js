document.addEventListener('DOMContentLoaded', () => {
    // Basic initialization for the student dashboard.
    // In a real application, we would fetch the user's data (workouts, next bill) here.
    console.log("Student dashboard loaded");
});
